To compile with dynamic scoping, run "make all MOREFLAGS=' -D=DYNAMIC '". To run the dynamic scoping tests, run "tl bootstrap.cl dyntest.cl"

If compilation fails, try settng the CC variable. Usually the problem is the wrong C++ compiler.
